apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authserver-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.databaseVolumeSize }}
